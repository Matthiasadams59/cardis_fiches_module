# cardis_fiches_module
Projet Fiches Modules A2 ISEP

Comment faire un merge de sa branche jusque master : 
```
git checkout master
git pull origin master
git merge <votre branche>
git push origin master
```

